server:
  port: 8085
spring:
  application:
    name: ledger-service
  # datasource:
  #   url: jdbc:h2:mem:ledgerdb;DB_CLOSE_DELAY=-1
  #   driver-class-name: org.h2.Driver
  #   username: sa
  #   password:
  # jpa:
  #   database-platform: org.hibernate.dialect.H2Dialect
  #   hibernate:
  #     ddl-auto: create-drop
  #   show-sql: true
  # h2:
  #   console:
  #     enabled: true
  #     path: /h2-console

  datasource:
    url: jdbc:postgresql://localhost:5432/safipay_ledger
    driver-class-name: org.postgresql.Driver
    username: rethabile
    password: password123
    hikari:
      maximum-pool-size: 10
      minimum-idle: 5
  
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
jwt:
  secret: safipay-super-secret-key-for-jwt-signing-must-be-256-bits-long
services:
  wallet-service-url: http://localhost:8082
  payment-service-url: http://localhost:8083
logging:
  level:
    com.safipay.ledger: DEBUG
